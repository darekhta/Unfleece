---
import Base from '../layouts/Base.astro';
import HomePage from '../components/HomePage.astro';
import { homeAlternates, localizedPath } from '../lib/locales';
---

<Base lang="en" canonicalPath={localizedPath('en', '/')} alternates={homeAlternates()}>
  <HomePage locale="en" />
</Base>
